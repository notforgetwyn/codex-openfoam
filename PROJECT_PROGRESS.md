# FoamDesk 项目进度汇报

## 一、已经完成的工作

### 1. 绘制几何页面

目前绘制几何页面已经具备基础几何建模能力，可以创建和管理常见几何体，包括立方体、球体、圆柱、圆锥、机翼和弯管。用户可以在模型树中查看当前几何体，并通过勾选控制几何体是否显示。

页面已经支持导入 STL、导出 STL、完成绘制等流程。其中“完成绘制”会把当前勾选的几何体导入到网格生成页面，作为后续 OpenFOAM 网格生成的几何输入。绘制几何页面还实现了按 case 保存几何状态，不同 case 之间的几何数据可以相互独立。

几何体显示方面，已经从原来的单纯实体显示改为“实体面 + 边线 + 顶点”的显示方式。这样用户在编辑几何体时可以看到模型由哪些三角面、边和点组成，更便于理解点、线、面编辑的对象。

点、线、面编辑方面，已经从最初的弹窗输入 `dx dy dz` 改为交互式编辑。现在用户可以进入按点移动、按线移动或按面拉伸模式，在 3D 视图中点选目标点、线或面，然后通过三轴操纵器进行移动。

### 2. 三轴操纵器编辑

已经实现第一版三轴操纵器功能。用户点选点、线或面后，界面会在选中区域显示红、绿、蓝三根轴：

- 红色轴表示 X 方向
- 绿色轴表示 Y 方向
- 蓝色轴表示 Z 方向

用户拖动某一根轴时，几何体只会沿对应方向移动。该功能用于替代原来不直观的输入式编辑，使绘制几何页面更接近 CAD 建模软件的使用方式。

在此基础上，已经补充“方案 B”的数值编辑能力。右侧属性栏增加了“三轴编辑位移”区域，包含 X、Y、Z 三个数值输入框和“应用位移”按钮。用户拖动三轴操纵器时，位移数值会写回到该区域；用户也可以手动输入位移数值，再点击“应用位移”进行精确编辑。

### 3. 网格生成页面

网格生成页面已经能够接收绘制几何页面导入的 STL 几何体，并用于后续网格生成流程。边界配置表已从仿真参数配置页面迁移到网格生成页面，使边界条件和网格边界面对应关系更加清晰。

当前网格生成页面中的边界表已经可以配置边界类型、边界名称、速度和压力等信息，并在生成网格时写入 OpenFOAM 的 `0/U` 和 `0/p` 文件。对于设置了非零速度的边界面，页面中还增加了速度方向箭头显示，用于辅助用户理解入口速度方向。

### 4. 仿真参数配置页面

仿真参数配置页面已经增加流体类型和时间类型选择。当前支持不可压缩和可压缩两类流体设置，也支持稳态和瞬态求解类型。页面会根据流体类型和时间类型推荐对应求解器，例如 `simpleFoam`、`pimpleFoam`、`pisoFoam`、`rhoSimpleFoam`、`rhoPimpleFoam` 等。

不可压缩和可压缩参数显示逻辑已经做了区分。不可压缩流体只显示密度、运动粘度和动力粘度等基本物性；可压缩流体会额外显示温度、定压比热、气体常数和 Prandtl 数等参数。压力单位也已经区分，不可压缩时使用运动压力单位，可压缩时使用 Pa。

### 5. 求解运行页面

求解运行页面已经支持启动、停止和继续运行。求解进度条的逻辑也已经修正，不再强制在结束时跳到 100%，而是根据用户设置的计算范围显示实际进度。

页面可以通过 OpenFOAM 命令运行求解器，并实时显示标准输出和错误输出。后续结果导出流程中也已经接入 `foamToVTK`，用于为结果展示页面提供 VTK 数据。

### 6. 结果展示页面

结果展示页面目前已经支持表面云图、切片和流线等显示方式。流线显示已经做过多轮调整，目前支持入口面流线和 STL 附近流线两种显示模式，并支持基线数设置。

流线动画已经改为逐步生成，不是一开始就全部显示。播放动画时流线会逐渐出现，暂停后停止增长。此前流线中的粒子球效果已经移除。

### 7. FreeCAD 辅助工具

已经在 D 盘安装 FreeCAD 1.1.1，路径为：

```text
D:\freeCAD\FreeCAD\bin\freecad.exe
```

已经在桌面创建 FreeCAD 快捷方式，方便后续使用 FreeCAD 建立复杂几何模型，然后导出 STL 给 FoamDesk 使用。

## 二、正在进行的工作

当前主要正在完善绘制几何页面的交互式建模能力。重点是让点、线、面编辑从简单的输入式修改，逐步变成更接近 CAD 软件的三维交互编辑。

目前已经完成三轴移动操纵器和数值位移输入，但仍需要进一步测试实际使用体验，重点检查以下问题：

- 鼠标是否容易点中目标点、线或面
- 三轴箭头是否出现在正确位置
- 拖动红、绿、蓝轴时方向是否符合预期
- 手动输入 X、Y、Z 位移后，几何体是否能正确更新
- 编辑后的几何体是否能正常导出 STL
- 编辑后的 STL 是否能被网格生成页面正确导入

同时，当前交互编辑还只是移动/拉伸类型，没有加入旋转环、缩放手柄、撤销恢复、吸附网格等更高级 CAD 功能。

## 三、下一阶段计划

### 1. 完善三轴操纵器

下一阶段优先完善三轴操纵器的稳定性和可用性。需要重点优化选中反馈、箭头大小、拖拽灵敏度和高亮效果。用户点击点、线或面后，应该能明确看到当前选中了哪个局部区域。

后续可以增加以下能力：

- 选中区域高亮显示
- 拖动过程中显示实时位移数值
- 增加取消编辑按钮
- 增加撤销功能
- 支持局部坐标系和全局坐标系切换

### 2. 增加旋转和缩放操纵器

当前三轴操纵器只支持沿 X、Y、Z 移动。后续可以继续扩展为完整 CAD Gizmo：

- 平移箭头
- 旋转圆环
- 缩放方块

这样用户不仅可以移动点、线、面，也可以对局部区域做旋转和缩放变形。

### 3. 强化几何编辑和布尔运算

后续需要继续完善布尔运算和局部编辑后的几何修复能力。尤其是布尔差集、并集、交集之后，需要保证生成的 STL 是封闭、法向正确、没有重复点和破面的。

下一阶段可以增加几何检查功能：

- 检查是否封闭
- 检查是否存在非流形边
- 检查是否存在重复点
- 检查法向是否一致
- 一键清理和修复 STL

### 4. 加强 FreeCAD 与 FoamDesk 的配合

FoamDesk 不适合完全重写 FreeCAD 的全部建模能力。因此后续可以考虑把 FreeCAD 作为外部复杂建模工具，FoamDesk 负责导入、网格生成、仿真配置、求解运行和结果展示。

可以增加以下功能：

- 从 FoamDesk 打开 FreeCAD
- 从 FreeCAD 导出的 STL 自动导入当前 case
- 支持 STEP/STL 文件管理
- 在 FoamDesk 中记录外部几何文件来源

### 5. 完善仿真配置生成

仿真参数配置页面已经支持可压缩和不可压缩，但后续还需要继续完善不同求解器对应的 OpenFOAM 文件生成逻辑，确保不同流体类型、时间类型和湍流模型组合下，生成的 `0`、`constant`、`system` 文件都完整正确。

重点包括：

- 不同求解器对应的字段文件
- 湍流模型字段完整性
- 可压缩热物性文件
- 边界条件完整覆盖
- 配置合法性检查

### 6. 完善结果展示体验

结果展示页面下一阶段需要继续优化流线和切片交互体验。尤其是流线显示，需要继续测试不同 STL、不同速度场、不同入口出口配置下是否稳定。

可以继续增强：

- 流线密度自适应
- 流线方向自动判断
- 流线动画速度控制
- STL 附近流线过滤
- 残差曲线和结果视图联动

## 四、当前建议

短期内建议先集中测试绘制几何页面的三轴操纵器，确认基本交互能否满足论文演示和项目展示需求。如果三轴拖拽稳定，再继续做旋转、缩放和撤销功能。

复杂工业级建模建议继续交给 FreeCAD 完成，FoamDesk 主要负责 OpenFOAM 仿真流程整合。这样可以避免在项目中重复开发完整 CAD 系统，把精力集中在 CFD 前处理、求解运行和结果可视化上。

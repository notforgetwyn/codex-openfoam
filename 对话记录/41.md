## 阶段 14 修复：移除顶部工具栏和左侧活动栏

当前阶段目标：
- 按用户截图要求，删除主界面红色圈出的两个区域。
- 删除顶部工具栏，减少重复按钮。
- 删除左侧活动栏，保留项目树作为主要导航。
- 本阶段不改变项目选择、求解、结果和可视化业务逻辑。

本阶段完成功能：
- 主界面不再显示顶部工具栏。
- 主界面不再显示左侧活动栏。
- 主界面布局改为：菜单栏 + 左侧资源管理器 + 右侧工作区 + 底部日志面板。
- `项目选择`、`新建项目`、`打开项目`、`保存设置`、`运行`、`停止` 等功能仍保留在顶部菜单栏中。
- 工作区横向空间变大，项目树和主内容区域更清晰。

本阶段页面与 UI 完成情况：

页面名 `MainWindow`：
- 页面标题：`FoamDesk`。
- UI 组件：`WindowTitleBar`、`QMenuBar`、`QSplitter`、项目树、工作区、底部日志面板。
- 已移除组件：
  - 顶部 `QToolBar`。
  - 左侧活动栏 `activityBar`。
- 按钮或菜单项：
  - `文件 -> 新建项目` 保留。
  - `文件 -> 打开项目` 保留。
  - `文件 -> 返回项目选择` 保留。
  - `求解器 -> 运行最小仿真` 保留。
  - `求解器 -> 停止当前任务` 保留。
  - `工具 -> 设置` 保留。
- 键盘操作：本阶段没有新增快捷键。
- 鼠标操作：用户通过菜单栏和项目树完成操作。
- 状态提示：状态栏和日志面板不变。

本阶段完成设计：
- 主布局 `workbench` 从三栏改为两栏。
- 原布局：活动栏 + 资源管理器 + 工作区。
- 新布局：资源管理器 + 工作区。
- 顶部工具栏不再加入 `shell_layout`。
- 保留 `_build_toolbar()` 和 `_build_activity_bar()` 方法，暂不删除代码，方便后续如果要做设置开关可以恢复。

本阶段数据流设计：
- UI 布局变化不影响业务数据流。
- 项目选择仍通过菜单栏进入。
- 求解运行仍通过菜单栏或页面按钮触发。
- 项目树点击仍调用 `_activate_project()`。
- 已有结果恢复逻辑不变。

本阶段核心业务场景完成进度：
- UI 简化业务场景：完成顶部工具栏和左侧活动栏移除。
- 项目管理业务场景：入口保留在菜单栏和项目树。
- 求解运行业务场景：未变更。
- 结果管理业务场景：未变更。
- 可视化业务场景：未变更。
- 未完成内容：如果后续需要，可以增加“简洁模式/完整模式”设置项，让用户选择是否显示工具栏和活动栏。

涉及文件：
- `src/foamdesk/ui/main_window.py`
- `对话记录/41.md`

验证情况：
- 已执行语法检查：`python3 -m py_compile src/foamdesk/ui/main_window.py`。
- 已执行单元测试：`PYTHONPATH=src python3 -m pytest tests/unit -q`。
- 单元测试结果：`24 passed in 1.34s`。
- 需要人工验证：启动程序确认顶部工具栏和左侧活动栏已消失。
- GitHub 提交和推送将在本报告写入后执行。

下一阶段建议：
- 当前 Sprint：Sprint 14 修复：移除顶部工具栏和左侧活动栏。
- 下一个 Sprint：Sprint 16：可视化字段选择器与时间步选择器。

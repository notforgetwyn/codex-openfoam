# 清理 main_window 相关无引用代码

日期：2026-05-26

## 本次主题

检查 `src/foamdesk/ui` 下 6 个以 `main_window` 开头的 Python 文件，删除已经没有代码入口引用的旧逻辑。

## 检查范围

- `src/foamdesk/ui/main_window.py`
- `src/foamdesk/ui/main_window_geometry_logic.py`
- `src/foamdesk/ui/main_window_parameters_logic.py`
- `src/foamdesk/ui/main_window_project_logic.py`
- `src/foamdesk/ui/main_window_results_logic.py`
- `src/foamdesk/ui/main_window_settings_physics_logic.py`

## 本次删除

- 删除旧几何/CAD 页面遗留函数：
  - `_read_stl_transform_dialog()`
  - `_draw_stl_transform_preview()`
  - `_read_draw_geometry()`
  - `_apply_domain_template()`
  - `_apply_boundary_conditions()`
  - `_run_preflight_check()`
  - `_import_template_stl()`
  - `_edit_imported_stl_transform()`
  - `_pick_stl_asset_dialog()`
  - `_preview_imported_stl()`
  - `_show_cad_import_limitations()`
  - `_select_stl_asset_for_snappy()`
- 删除旧 activity bar 的 `_on_activity_changed()`。
- 删除旧 Matplotlib/兼容 VTK 入口 `_ensure_vtk_viewer()`。
- 删除旧矢量范围辅助函数 `_vector_magnitude_range()`。
- 清理随之无用的导入。

## 保留内容

- 保留当前 `MainWindow` 仍然导入的 5 个 mixin 文件。
- 保留当前 UI 和业务仍在使用的函数，例如绘制几何、blockMeshDict/STL 生成、一键仿真流水线、Native VTK 结果显示、参数和设置逻辑。
- 保留一键仿真仍依赖的 `_auto_stl_asset_for_snappy()` 和 `_read_snappy_settings()`。

## 验证

- 已用静态引用计数检查 6 个 `main_window*.py` 文件，没有发现新的 `count <= 1` 疑似无引用方法。
- 已确认删除的函数名在 `src/foamdesk/ui` 下没有残留引用。
- 已执行 `python3 -m compileall src/foamdesk`。
- 已执行 `git diff --check`。

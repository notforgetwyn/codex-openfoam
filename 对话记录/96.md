# 删除 main_window.py 中未接入实际程序的代码

日期：2026-05-26

## 主题

清理 `src/foamdesk/ui/main_window.py` 中已确认没有接入实际界面或调用链的旧函数与预留代码。

## 正文

本次删除内容包括：

- `_build_toolbar`
- `_build_activity_bar`
- `_make_text_panel`
- `_build_geometry_tab`
- `_apply_custom_domain`
- `_finalize_vtk_render`
- `_run_full_pipeline`
- 未接入的快捷 tab 跳转函数：
  - `_open_solver_select_tab`
  - `_open_parameter_tab`
  - `_open_solver_run_tab`
  - `_open_simulation_prepare_tab`
  - `_open_physics_prepare_tab`
  - `_open_geometry_tab`

同步移除了对应不再使用的 PySide6 import：

- `QToolBar`
- `QListWidget`
- `QListWidgetItem`

保留仍在实际程序中使用的函数，例如设置页、环境页、绘制几何页、网格生成页入口，以及旧 `VtkViewerDialog` 中仍被 STL 预览和报告导出使用的部分。

## 验证

已执行：

```bash
python3 -m compileall src/foamdesk
git diff --check
```

结果：均通过。

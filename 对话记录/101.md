# 精简结果可视化字段

日期：2026-05-26

## 主题

从仿真结果可视化字段列表中移除较高级/暂不展示的变量，只保留当前阶段常用基础字段。

## 本次修改

- 结果可视化下拉框保留：
  - `U`
  - `mag(U)`
  - `p`
  - `T`
  - `k`
- 从结果可视化下拉框和单位映射中移除：
  - `epsilon`
  - `omega`
  - `nut`
  - `yPlus`
  - `wallShearStress`
  - `vorticity`
  - `Q`
  - `alpha.water`
- 从结果索引服务的可展示字段文件集合中移除：
  - `epsilon`
  - `omega`
  - `nut`

## 保留说明

`project_service.py` 中仍保留 `epsilon` 和 `nut` 的 OpenFOAM case 文件生成逻辑，因为 `simpleFoam + kEpsilon` 等湍流模型运行时可能需要这些文件。它们不再作为当前结果可视化字段展示，但不应删除求解器所需的 case 支持逻辑。

## 验证

- 已执行 `python3 -m compileall src/foamdesk`，通过。
- 已执行 `git diff --check`，通过。
- 已搜索相关字段名，确认 UI 可视化字段列表中已移除。


# 删除几何/CAD 菜单栏

日期：2026-05-26

## 本次主题

删除主菜单栏中的“几何/CAD”菜单及其直接关联代码。

## 本次修改

- 删除 `geometry_menu = menu_bar.addMenu("几何/CAD")`。
- 删除该菜单下的 STL 导入、网格生成、snappyHexMesh、checkMesh、一键前处理等 action 绑定。
- 删除只服务于该菜单的 `_open_mesh_generation_tab()` 方法。
- 删除原菜单 action 对应的触发入口函数：
  - `_import_stl_geometry()`
  - `_generate_snappy_hex_mesh_dict()`
  - `_run_snappy_hex_mesh()`
  - `_run_check_mesh()`
  - `_run_preprocess_pipeline()`
- 删除“可视化网格生成”页中继续引用这些入口函数的按钮。
- 调整“可视化网格生成”页说明文字，使其不再描述已经移除的手动 snappy/checkMesh 按钮流程。
- 将项目主页建议文案中的“几何/CAD”改为现有“可视化网格生成”页面。

## 保留内容

- 保留“刷新网格状态”“打开绘制几何”“预览计算域/STL”三个页面入口。
- 保留被一键仿真流程仍然依赖的底层辅助函数，例如 snappy 设置读取、自动选择 STL、生成求解前支持文件等。

## 验证

- 已确认源码中不再存在 `几何/CAD`、`geometry_menu`、`_open_mesh_generation_tab`、原菜单 action 触发函数引用。
- 已执行 `python3 -m compileall src/foamdesk/ui/main_window.py src/foamdesk/ui/main_window_geometry_logic.py src/foamdesk/ui/main_window_project_logic.py`。
- 已执行 `git diff --check`。

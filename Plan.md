v一、先定整体布局（你要求：上下两大区域）
你的网格生成标签页 = 上侧参数配置区 + 下侧 VTK 3D 可视化区，整体结构固定，适配操作逻辑：
plaintext
【标签页：可视化生成网格】
┌─────────────────────────────────────────────────────┐
│                  上区：参数配置面板（可滚动）         │
│  分组面板1  分组面板2  分组面板3  功能按钮区          │
├─────────────────────────────────────────────────────┤
│                                                     │
│                下区：VTK 3D 视图（网格预览）        │
│           显示原始几何 + 生成后的网格 + 边界面        │
│                                                     │
└─────────────────────────────────────────────────────┘
补充细节
上下区域用 QSplitter 分割，支持拖动调整高度；
上侧参数区内容多，外层套 QScrollArea，防止控件溢出；
所有参数按功能分组，用 QGroupBox 框起来，视觉清晰、符合工业软件习惯；
所有操作按钮统一放在参数区顶部 / 底部，集中操作。
二、上区：参数配置面板（核心：分 5 大功能组 + 按钮区）
按 操作先后顺序 排布分组，用户从上到下依次设置，逻辑连贯。
整体分区顺序（从上到下）
导入与几何管理组
边界面设置组（你提到的「面参数」，对接 OpenFOAM 边界）
背景计算域网格参数（对应 OpenFOAM blockMesh）
贴体网格加密参数（对应 OpenFOAM snappyHexMesh）
网格质量与导出参数
全局功能按钮栏（执行、预览、重置、导出）
逐个分组：控件、摆放、参数作用、UI 样式
组 1：导入与几何管理（最顶部）
作用：加载「几何绘制页」导出的模型 / STL，管理所有几何部件
控件布局（横向排列）：
按钮：导入几何（加载本软件绘制的模型 / 外部 STL）
下拉框：当前选中部件（列出所有几何体，切换选中对象）
复选框：显示原始几何 / 半透明显示
按钮：清空几何
说明
导入后，下侧 3D 视图立刻加载模型；
下拉框同步左侧模型树，选中部件会在 3D 视图高亮。
组 2：边界面参数设置（你的核心需求：给面设参数）
作用：给几何体表面划分边界（入口 / 出口 / 壁面 / 对称面），对应 OpenFOAM 边界条件，仿真必备
用 QGroupBox 标题：边界面配置
布局：左侧操作区 + 右侧参数区
左侧（拾取 + 选择）
单选按钮组：选择模式 → 【点选面】/【框选面】
按钮：拾取面（激活 VTK 面拾取功能，鼠标在 3D 视图选面）
按钮：取消选中 / 清空所有边界
右侧（边界属性配置）
下拉框：边界类型（固定选项，直接对应 OpenFOAM）
可选列表：
速度入口 inlet
压力出口 outlet
固壁 wall
对称面 symmetry
计算域外边界 patch
输入框：边界名称（自定义命名，最终生成对应 STL 文件名）
按钮：应用到选中面（把类型 + 名称绑定到选中面片）
表格控件：已定义边界列表
列：边界名称 | 边界类型 | 面片数量 | 操作 (删除)
使用逻辑
点「拾取面」→ 去下方 3D 视图点击模型表面；
右侧选边界类型、填名称；
点「应用」→ 写入表格，3D 视图用不同颜色区分不同边界。
组 3：背景计算域网格参数（blockMesh 核心参数）
标题：基础计算域网格（背景网格）
作用：定义整个仿真区域的大小、基础网格密度，对应 OpenFOAM blockMeshDict
分两列摆放：标签 + 数值输入框
计算域尺寸（坐标范围）
X 最小 / X 最大
Y 最小 / Y 最大
Z 最小 / Z 最大
（默认自动读取你几何的包围盒，支持手动修改）
网格划分数量（网格分辨率）
X 方向网格数
Y 方向网格数
Z 方向网格数
辅助选项
复选框：正交网格
下拉框：网格单位（米 / 毫米，和建模统一）
组 4：贴体网格加密参数（snappyHexMesh 核心）
标题：模型贴体网格加密
作用：让网格贴合曲面、局部加密，流体仿真近壁面必须加密
全局加密等级（下拉框 / 滑动条：1~5 级，等级越高网格越密）
壁面边界层设置（流体仿真重点）
输入框：边界层层数
输入框：第一层网格高度
输入框：层增长比例
局部加密
复选框：启用局部加密
下拉框：选中部件加密等级
复选框：保留原始边界轮廓
组 5：网格质量控制参数
标题：网格质量约束
作用：过滤劣质网格，防止 OpenFOAM 计算报错
输入框：最大网格畸变率（阈值，超过则剔除）
输入框：最小网格体积
复选框：自动删除负体积网格
复选框：网格平滑处理
组 6：底部功能按钮栏（整行排布，醒目）
所有核心执行按钮，放在参数区最底端：
生成预览网格 → 仅在下方 3D 视图预览网格，不生成文件
检查网格质量 → 弹窗提示：网格合格 / 存在问题
导出网格配置文件 → 生成 blockMeshDict /snappyHexMeshDict
导出STL(按边界拆分) → 按你定义的边界，自动拆分多个 STL（直接扔进 OpenFOAM triSurface）
重置所有参数
三、下区：VTK 3D 可视化视图（纯展示 + 交互）
功能 & 样式
全屏 VTK 渲染窗口，继承你之前的交互：
相机模式（C）/ 对象模式（A）切换
左键旋转、中键平移、滚轮缩放
图层显示规则：
原始几何：半透明浅灰色
生成的网格：白色线框（优先显示）
不同边界面：用不同纯色填充（入口红、出口蓝、壁面灰）
附加小控件（悬浮 / 视图角落）
视角快捷按钮：正视 / 俯视 / 等轴测 / 重置视角
显示模式切换：【实体】/【线框】/【仅网格】
交互联动规则
在上侧「拾取面」激活时，鼠标点击视图面片 → 自动选中并高亮；
切换边界列表，视图同步高亮对应区域；
点击「生成预览网格」，视图实时刷新网格效果。
四、完整操作流程（从加载几何 → 完成网格配置，手把手步骤）
结合你的软件，模拟用户完整操作链路，同时对应每一步 UI 操作：
步骤 1：加载几何
进入「可视化生成网格」标签页 → 点击【导入几何】→ 读取「几何绘制页」建好的模型；
下方 3D 视图显示模型，顶部下拉框加载所有部件列表。
步骤 2：划分边界面（最重要，对接 OpenFOAM）
选择拾取模式：点选面；
点击【拾取面】，到 3D 视图点击模型的入口表面；
右侧下拉框选择边界类型：速度入口，填写名称 inlet；
点击【应用到选中面】→ 表格新增一条记录，视图该面变红；
重复操作：依次选中出口、壁面、计算域外边界，分配对应类型；
步骤 3：配置背景计算域网格
查看自动填充的 XYZ 坐标范围（可手动微调，扩大计算域）；
设置 XYZ 三个方向的网格数量（数量越大，网格越密、计算越慢）。
步骤 4：配置贴体加密 & 边界层
设置全局加密等级；
开启边界层，填写层数、第一层高度（流体仿真必调）；
如需局部细化，选中部件单独设置加密等级。
步骤 5：网格质量约束
保留默认阈值，新手不用改；复杂模型可适当放宽畸变率。
步骤 6：预览 + 检查
点击【生成预览网格】→ 下方视图显示完整线框网格；
点击【检查网格质量】→ 查看是否有坏网格，有则返回修改参数。
步骤 7：导出文件（对接 OpenFOAM）
【导出 STL (按边界拆分)】→ 自动生成 inlet.stl/wall.stl 等；
【导出网格配置文件】→ 生成 blockMeshDict、snappyHexMeshDict；
把所有文件拷贝到 OpenFOAM 算例对应目录，直接运行 blockMesh / snappyHexMesh。